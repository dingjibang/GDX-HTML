group 'team.rpsg.html'
version '1.0'

apply plugin: 'groovy'

repositories {
    mavenCentral()
}

dependencies {
    compile 'org.codehaus.groovy:groovy-all:2.3.11'
}

sourceSets {
    main {
        java { srcDirs = [] }
        groovy { srcDirs = ["src/"] }
    }
}